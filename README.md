# README.md
项目：test
简介：这是一个用于试验和练习的小仓库，包含一个简单的 Python 示例、对应的测试和基础说明。可以用来展示如何运行示例与运行测试。

运行示例：
- 安装依赖：pip install -r requirements.txt（目前本示例不需要额外依赖）
- 运行：python src/hello.py

文件列表：
- src/hello.py: 一个简单的问候函数和命令行入口
- tests/test_hello.py: 对 hello.greet 的单元测试
